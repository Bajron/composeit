# TODO: better Linux detection?
COUNT_OPT="${WINDIR+-n}${SYSTEMD_EXEC_PID+-c}"
# On Linux, it is default, so -n for no DNS just to have an option here
KEEP_OPT="${WINDIR+-t}${SYSTEMD_EXEC_PID+-n}"

MAX_REPEAT=5
